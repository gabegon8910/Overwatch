# =============================================================================
# Overwatch — Environment Configuration
# =============================================================================
# Copy this file to .env and update the values below.
#
#   cp .env.example .env
#
# IMPORTANT: You MUST change SECRET_KEY, ENCRYPTION_KEY, and POSTGRES_PASSWORD
# before running in production.
# =============================================================================

# -- Docker Images ------------------------------------------------------------
# GitHub Container Registry owner and image version
GITHUB_OWNER=gabegon8910
VERSION=latest

# -- Database -----------------------------------------------------------------
POSTGRES_USER=ssm_user
POSTGRES_PASSWORD=CHANGE_ME_RANDOM_PASSWORD
POSTGRES_DB=ssm_db

# -- Security -----------------------------------------------------------------
# Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# Must be exactly 32 characters. Generate with: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_ME_32_CHAR_KEY_HERE_YEAH

# -- Network ------------------------------------------------------------------
# Public URL where users access Overwatch (used for OAuth, email links, CORS)
FRONTEND_URL=http://localhost

# Port exposed on the host for the web UI
HTTP_PORT=80

# -- Licensing ----------------------------------------------------------------
# Leave LICENSE_KEY empty for the free tier.
# Purchase a Pro license at https://byteforce.us for:
#   - Secrets Vault, Vulnerability Scanning, Agent Mode
LICENSE_SERVER_URL=https://ow-license1.byteforce.us/v1
LICENSE_KEY=
